/**
 * Página de trocas de serviço (stub).
 */

import { StubPage } from '../components/StubPage';

export function TrocaServicoPage() {
  return (
    <StubPage
      titulo="Troca de Serviço"
      descricao="Solicitação, aceite pelo alvo e aprovação pelo Subtenente. Funcionalidade em desenvolvimento."
    />
  );
}
