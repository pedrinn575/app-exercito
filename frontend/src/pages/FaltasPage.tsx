/**
 * Página de controle de faltas (stub).
 */

import { StubPage } from '../components/StubPage';

export function FaltasPage() {
  return (
    <StubPage
      titulo="Controle de Faltas"
      descricao="Faltas justificadas e não justificadas com substituição de reserva. Funcionalidade em desenvolvimento."
    />
  );
}
